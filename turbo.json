{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "generate:codama-idl": {
      "outputs": ["idl/codama.json"]
    },
    "generate:clients": {
      "dependsOn": ["generate:codama-idl"],
      "outputs": ["clients/ts/**"]
    },
    "typecheck": {
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build", "typecheck"],
      "outputs": ["dist/**"]
    }
  }
}
